# ☸️ Kubernetes Basic Lab - Helm Practice

## ✅ Objectives

- Learn how to deploy Kubernetes resources using `Helm`.
- Install an Nginx web server via a Helm chart.
- Practice updating `values.yaml` and redeploying with `helm`.

---
