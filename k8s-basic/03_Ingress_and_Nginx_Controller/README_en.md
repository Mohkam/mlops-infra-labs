# ☸️ Kubernetes Basic Lab - Ingress + Nginx Controller

## ✅ Objectives

- Route multiple services using `Ingress` to expose them externally.
- Configure the Nginx Ingress Controller (e.g., `minikube addons enable ingress`).
- Implement path-based routing for services such as `/nginx` and `/httpd`.
