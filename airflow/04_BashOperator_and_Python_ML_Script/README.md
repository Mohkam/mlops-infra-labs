# ☸️ Airflow Lab - Running External Python ML Script with BashOperator

## ✅ Objectives

- Execute `train.py` as an external, standalone script outside of the DAG.
- Use Airflow's `BashOperator` to run the training script.
- Verify that the training produces `model.pkl` and inspect logs.
